{% extends 'base.html.twig' %}

{% block title %}Pai index{% endblock %}

{% block body %}
    <h1>Pai index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>File</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for pai in pais %}
            <tr>
                <td>{{ pai.id }}</td>
                <td>{{ pai.file }}</td>
                <td>
                    <a href="{{ path('pai_show', {'id': pai.id}) }}">show</a>
                    <a href="{{ path('pai_edit', {'id': pai.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('pai_new') }}">Create new</a>
{% endblock %}
